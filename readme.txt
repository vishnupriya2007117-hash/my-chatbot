Firstly to run this, Yall need to write (pip install streamlit google-genai)in the terminal, which installs streamlit pack which helps you open the local host website. 

API key is not required as i have put the API code in default, I have used gemini API basically.

Next, You need to write (py -m streamlit run app.py)-- Which runs the chatbot 

Thank you !!
 